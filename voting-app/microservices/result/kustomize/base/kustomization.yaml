apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
  
resources:
    - service.yaml
    - deployment.yaml
    - redis.yaml
    - redis-service.yaml
    - namespace.yaml
    - postgress-deploy.yaml
    - postgress-service.yml


generatorOptions:
 disableNameSuffixHash: true
