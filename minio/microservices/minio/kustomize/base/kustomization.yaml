apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
  
resources:
    - service.yaml
    - deployment.yaml
    - namespace.yaml
    - minio-pvc.yml
    
   

generatorOptions:
 disableNameSuffixHash: true
